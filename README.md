@likely/analytics-test

## Dependencies

* docker
* python
* default-jre
* default-jdk

## Instructions

```
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
docker-compose up -d
python run.py
```
